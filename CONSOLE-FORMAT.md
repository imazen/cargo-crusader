# Console Table Format Reference

This document defines the five-column console table format used by Cargo Crusader.

## Data Structure

```rust
pub struct OfferedRow {
    /// Baseline test result: None = this IS baseline, Some(bool) = baseline exists and passed/failed
    pub baseline_passed: Option<bool>,

    /// Primary dependency being tested (depth 0)
    pub primary: DependencyRef,

    /// Version offered for testing (None for baseline rows)
    pub offered: Option<OfferedVersion>,

    /// Test execution results for primary dependency
    pub test: TestExecution,

    /// Transitive dependencies using different versions (depth > 0)
    pub transitive: Vec<TransitiveTest>,
}

pub struct DependencyRef {
    pub dependent_name: String,       // "image"
    pub dependent_version: String,    // "0.25.8"
    pub spec: String,                 // "^0.8.52" (what they require)
    pub resolved_version: String,     // "0.8.91" (what cargo chose)
    pub resolved_source: VersionSource,  // CratesIo | Local | Git
    pub used_offered_version: bool,
}

pub struct OfferedVersion {
    pub version: String,  // "this(0.8.91)" or "0.8.51"
    pub forced: bool,     // true shows [≠→!] suffix
}

pub struct TestExecution {
    pub commands: Vec<TestCommand>,  // fetch, check, test
}

pub struct TestCommand {
    pub command: CommandType,  // Fetch | Check | Test
    pub features: Vec<String>,
    pub result: CommandResult,
}

pub struct CommandResult {
    pub passed: bool,
    pub duration: f64,
    pub failures: Vec<CrateFailure>,  // Which crate(s) failed
}
```

## Icon Legend

| Icon | Meaning | Usage |
|------|---------|-------|
| `✓` | Test passed | Test actually ran with offered version and succeeded |
| `✗` | Test failed | Test ran with offered version and failed |
| `⊘` | Skipped | Version was offered but not tested (resolved elsewhere) |
| `-` | Baseline | Indicates baseline test row |
| `=` | Exact match | Cargo resolved to exact offered version |
| `↑` | Upgraded | Cargo upgraded to newer compatible version |
| `≠` | Mismatch | Version incompatible or forced |
| `📦` | crates.io | Version from crates.io registry |
| `📁` | Local path | Version from local filesystem |

## Table Layout

```
┌────────────────────┬──────────┬─────────────────┬─────────────────────┬─────────────────────┐
│ Offered            │ Spec     │ Resolved        │ Dependent           │ Result         Time │
├────────────────────┼──────────┼─────────────────┼─────────────────────┼─────────────────────┤
│ {status} {res}{ver}│ {spec}   │ {ver} {src}     │ {name} {version}    │ {outcome} {ict} {t} │
└────────────────────┴──────────┴─────────────────┴─────────────────────┴─────────────────────┘
```

- **Offered**: `{status} {resolution}{version} [{forced}]`
- **Spec**: Dependency requirement or `→ {forced_spec}` when forced
- **Resolved**: `{version} {source_icon}` with optional `├─` tree rows
- **Dependent**: `{crate_name} {crate_version}` with optional `├─` tree rows
- **Result**: `{status} {ICT_marks}  {duration}s`

## Border Handling

**Error detail lines** (when tests fail):
- Error text spans columns 2-5 (entire middle section)
- Vertical borders only on far left (before column 1) and far right (after column 5)
- Above error: borders drop with corners `├──────────┘` and `└─────────────────────┘`
- Below error: full borders restored with `├──────────┬─────────────────┬─────────────────┬─────────────────┤`

**Example**:
```
│ ✗ =this(0.8.91)    │ ^0.8.52  │ 0.8.91 📁       │ image 0.25.8        │ REGRESSED ✓✗-  1.8s │
│                    ├──────────┘                  └─────────────────────┘                    │
│                    │ cargo check failed on image:0.25.8                                     │
│                    │   • error[E0425]: cannot find value `foo`                              │
│                    ├──────────┬─────────────────┬─────────────────────┬─────────────────────┤
```
_Note: Line 2 closes borders at column 2 and column 4 with corner pieces, creating a dropped panel effect_

**Multi-version tree rows**:
- Prefix with `├─` in Spec, Resolved, and Dependent columns
- Offered and Result columns remain empty

## Complete Demo Scenarios

### Demo 1: Simple Pass (Baseline + Offered, Both Pass)

```
┌────────────────────┬──────────┬─────────────────┬─────────────────────┬─────────────────────┐
│ Offered            │ Spec     │ Resolved        │ Dependent           │ Result         Time │
├────────────────────┼──────────┼─────────────────┼─────────────────────┼─────────────────────┤
│ - baseline         │ ^0.8.52  │ 0.8.51 📦       │ image 0.25.8        │ PASSED ✓✓✓     2.1s │
│ ✓ =this(0.8.91)    │ ^0.8.52  │ 0.8.91 📁       │ image 0.25.8        │ PASSED ✓✓✓     1.9s │
└────────────────────┴──────────┴─────────────────┴─────────────────────┴─────────────────────┘
```

**Data**:
- Row 1: `baseline_passed: None`, `offered: None`
- Row 2: `baseline_passed: Some(true)`, offered matched exactly

---

### Demo 2: Regression with Error Details

```
┌────────────────────┬──────────┬─────────────────┬─────────────────────┬─────────────────────┐
│ Offered            │ Spec     │ Resolved        │ Dependent           │ Result         Time │
├────────────────────┼──────────┼─────────────────┼─────────────────────┼─────────────────────┤
│ - baseline         │ ^0.8.52  │ 0.8.51 📦       │ image 0.25.8        │ PASSED ✓✓✓     2.1s │
│ ✗ =this(0.8.91)    │ ^0.8.52  │ 0.8.91 📁       │ image 0.25.8        │ REGRESSED ✓✗-  1.8s │
│                    ├──────────┘                  └─────────────────────┘                    │
│                    │ cargo check failed on image:0.25.8                                     │
│                    │   • error[E0425]: cannot find value `foo`                              │
│                    │   •    --> src/lib.rs:42:5                                             │
└────────────────────┴────────────────────────────────────────────────────────────────────────┘
```

**Border detail**: Line 3 spans columns 2-5, borders only on 2 & 4

---

### Demo 3: Natural Upgrade (Cargo Chose Different Version)

```
┌────────────────────┬──────────┬─────────────────┬─────────────────────┬─────────────────────┐
│ Offered            │ Spec     │ Resolved        │ Dependent           │ Result         Time │
├────────────────────┼──────────┼─────────────────┼─────────────────────┼─────────────────────┤
│ - baseline         │ ^0.8.40  │ 0.8.51 📦       │ image 0.25.8        │ PASSED ✓✓✓     2.1s │
│ ⊘ ↑this(0.8.91)    │ ^0.8.40  │ 0.8.95 📦       │ image 0.25.8        │ (not tested)        │
└────────────────────┴──────────┴─────────────────┴─────────────────────┴─────────────────────┘
```

**Data**: Offered but cargo resolved to different semver-compatible version

---

### Demo 4: Forced Version (Outside Semver Bounds)

```
┌────────────────────┬──────────┬─────────────────┬─────────────────────┬─────────────────────┐
│ Offered            │ Spec     │ Resolved        │ Dependent           │ Result         Time │
├────────────────────┼──────────┼─────────────────┼─────────────────────┼─────────────────────┤
│ - baseline         │ ^0.7.0   │ 0.7.5 📦        │ image 0.25.8        │ PASSED ✓✓✓     2.1s │
│ ✗ ≠this(0.8.91) [≠→!] │ → =0.8.91 │ 0.8.91 📁  │ image 0.25.8        │ REGRESSED ✓✗-  1.8s │
│                    ├──────────┘                  └─────────────────────┘                    │
│                    │ cargo check failed on image:0.25.8                                     │
│                    │   • error[E0432]: unresolved import `rgb::Pixel`                       │
└────────────────────┴────────────────────────────────────────────────────────────────────────┘
```

**Spec column**: Shows `→ =0.8.91` (forced spec) instead of original `^0.7.0`

---

### Demo 5: Multi-Version Tree (Multiple Depths)

```
┌────────────────────┬──────────┬─────────────────┬─────────────────────┬─────────────────────┐
│ Offered            │ Spec     │ Resolved        │ Dependent           │ Result         Time │
├────────────────────┼──────────┼─────────────────┼─────────────────────┼─────────────────────┤
│ - baseline         │ ^0.8.52  │ 0.8.51 📦       │ image 0.25.8        │ PASSED ✓✓✓     2.1s │
│ ✓ =this(0.8.91)    │ ^0.8.52  │ 0.8.91 📁       │ image 0.25.8        │ PASSED ✓✓✓     1.9s │
│                    │ ├─ ^0.8  │ ├─ 0.8.91 📁    │ ├─ pixels 0.14      │                     │
│                    │ ├─ =0.7.2│ ├─ 0.7.2 📦     │ ├─ legacy 1.0       │                     │
└────────────────────┴──────────┴─────────────────┴─────────────────────┴─────────────────────┘
```

**Data**: `transitive: [{ depth: 1, dependency: ... }, { depth: 1, ... }]`

---

### Demo 6: Multi-Version with Failure on Transitive

```
┌────────────────────┬──────────┬─────────────────┬─────────────────────┬─────────────────────┐
│ Offered            │ Spec     │ Resolved        │ Dependent           │ Result         Time │
├────────────────────┼──────────┼─────────────────┼─────────────────────┼─────────────────────┤
│ - baseline         │ ^0.8.52  │ 0.8.51 📦       │ image 0.25.8        │ PASSED ✓✓✓     2.1s │
│ ✗ =this(0.8.91)    │ ^0.8.52  │ 0.8.91 📁       │ image 0.25.8        │ REGRESSED ✓✗-  1.8s │
│                    ├──────────┘                  └─────────────────────┘                    │
│                    │ cargo check failed on pixels:0.14                                      │
│                    │   • error[E0061]: function takes 2 arguments                           │
│                    ├──────────┬─────────────────┬─────────────────────┬─────────────────────┤
│                    │ ├─ ^0.8  │ ├─ 0.8.91 📁    │ ├─ pixels 0.14      │                     │
│                    │ ├─ =0.7.2│ ├─ 0.7.2 📦     │ ├─ legacy 1.0       │                     │
└────────────────────┴──────────┴─────────────────┴─────────────────────┴─────────────────────┘
```

**Note**: Error shows which transitive crate failed, then lists all versions

---

### Demo 7: Skipped Version + Forced Alternative

```
┌────────────────────┬──────────┬─────────────────┬─────────────────────┬─────────────────────┐
│ Offered            │ Spec     │ Resolved        │ Dependent           │ Result         Time │
├────────────────────┼──────────┼─────────────────┼─────────────────────┼─────────────────────┤
│ - baseline         │ ^0.9.0   │ 0.9.1 📦        │ image 0.25.8        │ PASSED ✓✓✓     2.1s │
│ ⊘ ≠this(0.8.91)    │ ^0.9.0   │ 0.9.1 📦        │ image 0.25.8        │ (not tested)        │
│ ✗ ≠0.7.5 [≠→!]     │ → =0.7.5 │ 0.7.5 📦        │ image 0.25.8        │ REGRESSED ✓✗-  1.7s │
└────────────────────┴──────────┴─────────────────┴─────────────────────┴─────────────────────┘
```

**Data**: Row 2 skipped (incompatible), Row 3 forced to test anyway

---

### Demo 8: Broken Baseline (Both Fail)

```
┌────────────────────┬──────────┬─────────────────┬─────────────────────┬─────────────────────┐
│ Offered            │ Spec     │ Resolved        │ Dependent           │ Result         Time │
├────────────────────┼──────────┼─────────────────┼─────────────────────┼─────────────────────┤
│ - baseline         │ ^0.8.0   │ 0.8.51 📦       │ broken 1.0.0        │ BROKEN -✗-     1.3s │
│                    ├──────────┘                  └─────────────────────┘                    │
│                    │ cargo test failed on broken:1.0.0                                      │
│                    │   • error[E0599]: no method named `as_slice`                           │
│                    ├──────────┬─────────────────┬─────────────────────┬─────────────────────┤
│ ✗ =this(0.8.91)    │ ^0.8.0   │ 0.8.91 📁       │ broken 1.0.0        │ BROKEN -✗-     1.2s │
│                    ├──────────┘                  └─────────────────────┘                    │
│                    │ cargo test failed on broken:1.0.0                                      │
│                    │   • error[E0599]: no method named `as_slice`                           │
└────────────────────┴────────────────────────────────────────────────────────────────────────┘
```

**Data**: `baseline_passed: Some(false)` → offered row shows ✗ (both failed)

---

### Demo 9: Multiple Dependents with Separator

```
┌────────────────────┬──────────┬─────────────────┬─────────────────────┬─────────────────────┐
│ Offered            │ Spec     │ Resolved        │ Dependent           │ Result         Time │
├────────────────────┼──────────┼─────────────────┼─────────────────────┼─────────────────────┤
│ - baseline         │ ^0.8.52  │ 0.8.51 📦       │ image 0.25.8        │ PASSED ✓✓✓     2.1s │
│ ✓ =this(0.8.91)    │ ^0.8.52  │ 0.8.91 📁       │ image 0.25.8        │ PASSED ✓✓✓     1.9s │
├────────────────────┼──────────┼─────────────────┼─────────────────────┼─────────────────────┤
│ - baseline         │ ^0.8     │ 0.8.51 📦       │ pixels 0.14         │ PASSED ✓✓✓     1.5s │
│ ✓ =this(0.8.91)    │ ^0.8     │ 0.8.91 📁       │ pixels 0.14         │ PASSED ✓✓✓     1.4s │
└────────────────────┴──────────┴─────────────────┴─────────────────────┴─────────────────────┘
```

**Separator**: Full horizontal line between different dependents

---

## Rendering Logic

### Status Icon (Offered Column)
```rust
match (tested, baseline_passed, test_passed) {
    (false, _, _) => "⊘",  // Skipped
    (true, Some(true), true) => "✓",   // PASSED
    (true, Some(true), false) => "✗",  // REGRESSED
    (true, Some(false), _) => "✗",     // BROKEN (baseline failed, use ✗)
    (true, None, true) => "✓",         // PASSED (no baseline)
    (true, None, false) => "✗",        // FAILED (no baseline)
}
```

### Resolution Symbol (Offered Column)
```rust
if skipped {
    if semver_compatible { "↑" } else { "≠" }
} else if forced {
    "≠"
} else if offered == resolved {
    "="
} else {
    "≠"
}
```

### Spec Column
```rust
if forced {
    format!("→ ={}", offered_version)
} else {
    primary.spec
}
```

## CLI Integration

### Command-Line Flags
```bash
# Multiple versions (space-delimited, multiple args supported)
--test-versions "0.8.0 0.8.48" 0.8.91

# Force versions (bypass semver)
--force-versions "0.7.0" 0.9.0

# Features (passed to cargo)
--features "default serde" --features rgb/unstable
```

### Console Report Header
```
Testing 5 reverse dependencies of rgb
  this = 0.8.91 a138e69* (your work-in-progress version)
  features: default, serde

Legend: I=Install (cargo fetch), C=Check (cargo check), T=Test (cargo test)
        * = Version forced (tested outside dependent's stated semver requirements)
```

---

## Summary

This format provides:
- **Clear status indication**: Icons show what actually happened
- **Version tracking**: See what was offered vs. what resolved
- **Error context**: Know which crate failed and why
- **Multi-version awareness**: See all transitive dependencies
- **Forced testing**: Distinguish natural from forced resolution
